# LoopFlow R2M — 資料契約

本文件是 R2M 指令、設定、檔名、IFC 角色與跨軟體資料契約的權威來源。尚未定案事項在 `前期規劃/資料生態決策表.md`；**已決原則寫於此**。無明顯衝突時實作可調細節；衝突先改決策表再回寫本檔。

## 契約狀態

- 套用版本：尚未發 tag（文件骨架）
- 階段：產品邊界與自帶 IfcOpenShell 已決；指令名／磁碟檔名／IFC 元資料深度 **未凍結**
- 開發期指令清單：`rhino指令.md`（名稱為開發暫定）

## 核心裁決

| 主題 | 決定 | 狀態 |
|---|---|---|
| 產品邊界 | Rhino 發布建築殼 IFC；BIM 發布管線 IFC 當 Rhino 鎖定參考 | **已決** |
| 對口 | Revit、Archicad（3D）。無 3D 則不做 | **已決** |
| 交換格式 | **只 IFC**。不並行 DWG／USDZ／3DM 當主交換 | **已決** |
| BIM 外掛 | 不寫。用內建連結／匯出 | **已決** |
| Rhino IFC | 自帶 IfcOpenShell 0.8.4.post1 py39 Windows 輪子 + typing_extensions | **已決** |
| 設定／發布根 | `_LoopFlow_Config/loopflow_R2M/`（跟已存檔 `.3dm`） | **開發暫定** |
| Models 選取 | 對齊 R2B／R2O：排除記號 → 巢狀圖層樹 → 幾何類別勾選 | **已決方向；細節可議** |
| Inbound | 管線當參考；不當 Dictionary／可編輯建築；不自動改天花 | **已決** |
| `_SelAll` | 禁止 | **已決** |
| 來源檔 | 發布後還原選取／可見／`doc.Modified` | **已決** |
| 指令名／檔名 | 見下節候選 | **未凍結** |

## 共通規則

- 每種資料有 `schema_version`、producer、document 身分（sidecar 或 IFC 標頭；細節可議）。
- pending → validate → atomic；失敗不碰 last-good。
- 未知 schema／無法解析的 IFC：停止並說明，不猜測。
- Machine／API 名稱用穩定英文；**軟體介面一律英文**。文件與程式註解仍用繁中。

## 開發暫定名稱（未凍結）

Rhino 指令候選：`RMModels`、`RMInbound`、`RMOpen`。

磁碟（工作檔旁 `_LoopFlow_Config/loopflow_R2M/`）候選：

| 角色 | 路徑 |
|---|---|
| 設定 | `config.json` |
| log | `r2m.log` |
| 建築殼（覆寫） | `models/R2M.ifc`（pending 同層） |
| 對照表 | `models/R2M_map.json`（Rhino 物件 ↔ IFC GlobalId；是否第一版必出＝可議） |
| 管線參考 | `inbound/`（檔名規則可議） |

## IFC 元資料（可議）

建議預設（**尚未採用**，見決策表 R2M-ED-03）：

- 出去：幾何 + IFC 類型（IfcCovering／IfcWall／IfcSlab／IfcSpace 等）+ 穩定 GlobalId；對照寫 sidecar。
- 不在第一版承諾完整 property set、材料層、施工階段。
- 回來：以幾何外包絡為準；標稱管徑屬性僅供顯示，不當尺寸真相。

圖層如何對到 IFC 類型（命名慣例 vs 匯出時勾選）＝可議。

## 已知技術限制

- 原廠 Rhino 8 不能進出 IFC。
- IfcOpenShell **現行 PyPI 0.8.5 要 Python 3.10+**；Rhino 8 是 CPython 3.9。產品釘 **0.8.4.post1** `py39-none-win_amd64`，另需 `typing_extensions`。不可讓使用者 pip 最新版。輪子放套件內，執行時插入 `sys.path`，不寫進 `~\.rhinocode` site-packages。
- `ifcopenshell.api` 會要 shapely；第一版不依賴它。
- IFC 回 Rhino 多半是網面，夠對衝突與淨高，不保證毫米級管徑。針刺：天花底 280 cm、管頂 257.5 cm、淨空 22.5 cm（文件單位公分）。
- 載入 `_ifcopenshell_wrapper.pyd` 後，這台 Rhino 8.35 仍能 `LoadPlugIn(RhinoCycles)`。
