# LoopFlow R2M — 實作總覽

本文件是 R2M 實作範圍、架構邊界與完成關卡的唯一來源。詳細資料欄位見 `資料契約.md`，使用者操作順序見 `工作流程.md`，任務拆分見 `開發任務與路徑.md`，設定見 `系統設定.md`，即時狀態見 `重構進度.md`。

## 目前狀態

- 產品簡稱：R2M
- GitHub：`ChihyuTsai-Oli/LoopFlow_Rhino-to-MEP-Sync`（公開）
- 目標版本：尚未發 tag；`main` 為文件骨架
- Runtime（規劃）：Rhino 8／CPython 3.9／Windows
- 對口：Revit、Archicad（必須是 3D BIM）。沒有 3D 就不做
- 交換格式：**只 IFC**
- 開發模式：文件在 `wip/docs/`；針刺腳本在 `wip/tools/`；尚無產品 `wip/src/`
- 目前階段：**IfcOpenShell Rhino 端針刺已過**（0.8.4.post1／py39）。Revit／Archicad 連結尚未在這台電腦實機。未打 yak
- BIM 端：**不寫外掛**（用內建 IFC 連結／匯出）
- 跨產品：R2B／R2O 主鏈已發布；本產品不阻塞、也不合進那兩條整合分支

## 實作文件閱讀順序

1. `實作總覽.md`
2. `資料契約.md`
3. `工作流程.md`
4. `開發任務與路徑.md`
5. `系統設定.md`
6. `重構進度.md`

`rhino指令.md`：開發期指令名稱（**未凍結**）。  
`前期規劃/`：待決與過程；**不是**日常實作規格。若與本目錄六份衝突，以六份為準。

公開 `README.md` 是入口，不是實作權威。

## 產品邊界

```text
Rhino Producer（Models／Inbound／Open）
        ↓ 建築殼 IFC（pending → last-good）
Revit／Archicad（內建 IFC 連結，畫管）
        ↓ 管線 IFC（衝突段或勾選系統）
Rhino Inbound（鎖定參考層；不當建築物件）
        ↓ 人在 Rhino 改原天花／牆／板
Rhino 再發布下一輪建築殼
```

- 不把 BIM UI 或 IFC 規格實作塞進 LoopFlow 2.0／R2B／R2O。
- 不把管線寫進 Dictionary、Tag、出圖。
- 不自動降天花或套用「變更清單」；設計判斷只在 Rhino。
- 原廠 Rhino **不能**進出 IFC。產品**自帶 IfcOpenShell 0.8.4.post1**（開源庫，不是自寫 IFC 規格）。見決策表 R2M-ED-09。
- 不為 AutoCAD 2D／DWG 分叉。

## 完成關卡（摘要）

1. 六份正式文件與決策表（本輪）。
2. IfcOpenShell 在 Rhino 8／Python 3.9 實機針刺（**Rhino 端已過**；BIM 連結待補）。
3. Models：建築殼 IFC 安全發布，Revit／Archicad 能連結。
4. Inbound：管線 IFC 進鎖定參考層，可整批更新。
5. Open／Health、yak、公開使用說明。

細節任務見 `開發任務與路徑.md`。
