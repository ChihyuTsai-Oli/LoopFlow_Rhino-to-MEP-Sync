# R2M — 資料生態決策表

> 寬版閱讀檔：[資料生態決策表.html](資料生態決策表.html)。HTML 由 `wip/tools/fill_decision_table.py` 產生，**不要手動編輯 HTML**。

這是尚未定案事項的**唯一來源**。已決列寫「採用」與日期。確認前不寫正式產品功能碼（文件骨架與針刺除外）。

編號請保留。改「你的決定／說明」欄即可。

## 使用方式

- 「已決」表不做多 AI 比對，只記錄結果與日期。要翻案先在此表說明理由。
- 「待決」表有三個 AI 建議欄，每家各自填強度與選項（A／B／C），由你或多數決裁決。
- 每列在「現況／選項」欄給定 A／B／C，三家必須選同一組字母才比得起來。

### 三個 AI 建議欄怎麼讀

- **Cursor 建議（強度）**：2026-09-21 由 Cursor 在補齊實作前規格時填入，依據是已完成的 IfcOpenShell 針刺實測與已決的 ED-22～ED-28。
- **Claude 建議（強度）**：**尚未提供。**
- **Codex 建議（強度）**：**尚未提供。**

只有一家填寫時不會自動裁決；三家都填完再依下列規則處理。

### 自動裁決規則

對仍為「待決定」的列，依多數決填入「你的決定」：

1. **兩個以上強烈建議** → 採用；HTML 決定欄文字**白色**
2. **兩個以上一般建議** → 採用；HTML 決定欄文字**黃色**
3. **兩個以上輕鬆建議** → 採用；HTML 決定欄文字**綠色**

優先序為強烈 > 一般 > 輕鬆。「尚未提供」不計票。不足兩票同強度則維持待決定。你已手寫的決定列不會被覆寫；HTML 仍依三欄多數強度著色。

### AI 建議強度

| 強度 | 意義 |
|---|---|
| **強烈建議** | 關係資料正確、可復原、避免靜默錯誤；不採用需同等安全替代 |
| **一般建議** | 可改，但會增加規則／操作／維護成本 |
| **輕鬆建議** | 不影響核心鏈，可依習慣調整 |

## 已決

| # | 主題 | 決定 | 日期 |
|---|---|---|---|
| R2M-ED-01 | 產品形狀 | Rhino 出建築殼 IFC；BIM 出管線 IFC 當 Rhino 鎖定參考。設計判斷只在 Rhino。不自動降天花 | 2026-09-21 |
| R2M-ED-02 | 對口與格式 | 只 3D：Revit、Archicad。只 IFC。BIM 不寫外掛 | 2026-09-21 |
| R2M-ED-04 | Models 選取 | 對齊 R2B／R2O：排除記號（預設 `//`）→ 巢狀圖層樹 → 幾何類別勾選；禁止 `_SelAll`；來源還原。細節可議 | 2026-09-21 |
| R2M-ED-05 | Inbound 身分 | 管線是參考，不是 Dictionary／出圖物件 | 2026-09-21 |
| R2M-ED-06 | 獨立公開 repo | `LoopFlow_Rhino-to-MEP-Sync`；簡稱 R2M | 2026-09-21 |
| R2M-ED-09 | 自帶 IFC 庫 | 內嵌 IfcOpenShell **0.8.4.post1** `py39-win_amd64` 輪子 + `typing_extensions`。不 pip 最新（0.8.5 要 Python 3.10+）。不依賴 VisualARQ／BEAM。第一版用 `create_entity`／`geom.iterator`，不強制 `ifcopenshell.api`（會要 shapely） | 2026-09-21 |
| R2M-ED-22 | 單位 | IFC 一律公尺（`IfcSIUnit` METRE）。Rhino→IFC 乘 `UnitScale(doc.ModelUnitSystem, Meters)`，IFC→Rhino 乘倒數。數學寫進 `資料契約.md`，不重開 | 2026-09-21 |
| R2M-ED-23 | 座標基準 | 專案模型在原點附近，**不做基準點平移**；`IfcSite` 置於 `(0,0,0)`，Rhino 世界座標原樣輸出。遇到測量座標的案子再另開決策 | 2026-09-21 |
| R2M-ED-24 | IFC 版本 | IFC4，MVD `ReferenceView_V1.2` | 2026-09-21 |
| R2M-ED-25 | 物件身分 | IFC GlobalId 由 Rhino 物件 GUID 壓縮（`ifcopenshell.guid.compress`）。同一物件每次匯出得同一 GlobalId，不另建編號表。物件刪除重畫＝新身分 | 2026-09-21 |
| R2M-ED-26 | 樓層定義方式 | 沿用 LoopFlow 高程框形式（封閉曲線＋UserText 高程），但放在 **R2M 專屬圖層**、資料獨立，不讀 LoopFlow 的高程框。圖層名與欄位名另見待決 **ED-29** | 2026-09-21 |
| R2M-ED-27 | 高程基準 | R2M 用 **FL 結構面**（對齊 Revit／Archicad 標高慣例）。LoopFlow 用 FFL 完成面，兩者是不同來源，差一層面材厚度，**差異接受、由人工確認**，不做自動比對、不互相換算 | 2026-09-21 |
| R2M-ED-28 | Inbound 形式 | 管線當 **Worksession 外參**，不匯進工作檔。**第一版全手動**：空白 `.3dm` 讀 IFC → 手動另存 `.3dm` → 手動建 `.rws`。指令只負責建幾何，不寫檔、不掛載。自動化日後再評估 | 2026-09-21 |

## 待決

**擋住 C01 開工的是 ED-07、ED-13、ED-29。** 這三項不定就寫不出正確的 IFC。

| # | 主題 | 現況／選項 | Cursor 建議（強度） | Claude 建議（強度） | Codex 建議（強度） | 你的決定／說明 |
|---|---|---|---|---|---|---|
| R2M-ED-03 | 出去的 IFC 元資料要做到哪 | A 僅幾何；B 幾何＋IFC 類型＋GlobalId；C 再加樓層／空間／完整屬性集 | **一般建議**：採 B。沒有 IFC 類型，對方連在 Revit 裡過濾都做不到；但完整屬性集（材料層、防火時效、施工階段）是 BIM 端本來就要自己填的，第一版做進去投入產出不成比例，而且我們也沒有那些資料。GlobalId 已由 ED-25 定死，等於免費附帶。樓層不算在這一列，ED-13 會強制每個元件都有樓層。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-07 | 圖層怎麼對到 IFC 類型 | A 靠圖層命名慣例（例如 `IFC_Wall::…`）；B 匯出時對每個選到的圖層下拉選類型，記住上次；C 兩者並行，命名優先、未命中才問 | **強烈建議**：採 B。**擋 C01。** Rhino 的專案圖層是為了設計與出圖而分的，不是為 IFC 分的；A 等於要求使用者為了 R2M 重新整理整個圖層結構，而且改名之後會**靜默**出錯類型——牆變成天花不會報錯，只會在 BIM 端看起來怪怪的。B 每次都看得見自己選了什麼，記住上次就不會變成負擔。C 看似兩全，但「命名優先」意味著使用者不知道這一次到底走了哪條規則，出錯時最難查。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-08 | 第一版出不出 IfcSpace | A 不出；B 出，由使用者指定的封閉曲線／體生成；C 出，自動從牆圍出 | **一般建議**：採 A。MEP 工程師確實需要空間而不只是形狀，但 C 需要拓撲分析，而 Rhino 這邊的牆只是幾何、沒有牆的語意，圍不可靠；B 要使用者另外維護一組空間物件，等於再開一個像高程框那樣的資料來源，第一版還沒到那個成熟度。先讓對方靠建築殼幾何判斷，等 B03 拿到真實案例再回頭看缺不缺。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-10 | Inbound 怎麼篩 MEP | A 全讀不篩；B BIM 端先篩，Rhino 只顯示讀到的類別統計；C Rhino 端硬擋非 MEP 類 | **一般建議**：採 B。BIM 端先篩最有效，檔案小、讀得快，這也是 `工作流程.md` 已經寫給對方的要求。C 會誤傷：支架可能被出成 `IfcDiscreteAccessory`、套管可能是 `IfcBuildingElementProxy`，硬擋等於安靜丟掉你需要看的東西。改成讀進來後顯示「讀到哪些類別、各幾件」，讓你自己判斷對方是不是出錯範圍了。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-11 | 指令名與磁碟檔名何時凍結 | A 現在就凍；B B03 家中 BIM 驗證通過後凍；C C01 寫完後凍 | **輕鬆建議**：採 B。B03 有可能讓檔案結構改變（例如對方要求一次收多個 IFC），現在凍太早；但也不必拖到 C01 寫完，那時改名要動的程式比較多。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-12 | 測檔路徑環境變數 | A 現在就開 `LOOPFLOW_R2M_WORKFILES_ROOT`；B 等有正式測檔再開 | **輕鬆建議**：採 B。目前 fixtures 都在 repo 裡、體積很小，沒有放 Dropbox 的必要。等 B03 帶回真實 MEP IFC（可能很大、不適合進 Git）再開。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-13 | 物件掛到哪一層 | A 依範圍盒**底部 Z** 落在「本層 FL 到上層 FL」之間；B 依範圍盒**中心 Z**；C 依圖層命名指定樓層 | **強烈建議**：採 A。**擋 C01。** 底部 Z 對應「這個東西站在哪一層」的直覺，天花也正確——天花的底部 Z 在本層範圍內，會掛在自己那層。B 遇到跨層的垂直元件（管道間牆、樓梯、通高牆）會掉到錯的層，而且錯得很難發現。C 又回到 ED-07 的命名綁死問題。採 A 有一個必要條件：樓層表最上面必須有一個**頂部高程**當上界，否則最高層沒有上界，範圍判斷不成立。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-14 | IFC 專案／基地／建築名稱來源 | A 取 `.3dm` 檔名；B 每次對話框輸入；C 取檔名當預設、可改，存進 config | **輕鬆建議**：採 C。檔名多半就是案名，當預設省事；但 `.3dm` 常帶版本尾綴（`_rev03`），送到 BIM 端當專案名會很醜，所以要能改。存進 config 才不用每次重打。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-15 | Brep 轉三角網面的密度 | A 固定中等密度；B 對話框可選粗／中／細，預設中；C 跟隨文件的算繪網格設定 | **一般建議**：採 B。C 要避免——算繪網格是使用者的顯示偏好，同一個模型在兩台電腦會出不一樣的 IFC，違反可重現。A 夠簡單，但真實案子會遇到曲面天花需要細一點、或整棟模型需要粗一點。預設值等 B03 量到 BIM 端實際開啟速度再定。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-16 | 外參檔的圖層結構 | A 依 IFC 類型分層；B 依 MEP 系統（空調／給排水／電）分層；C 平鋪不分層 | **一般建議**：採 A。外參圖層在 Rhino 顯示成「檔名:圖層」，分層直接決定你能不能只開空調看衝突，所以 C 不考慮。B 更貼近使用習慣，但系統資訊要看對方有沒有填 `IfcSystem`，不保證拿得到；IFC 類型一定拿得到。等 B03 看到真實檔確實有系統資訊，再考慮改 B 或做成二選一。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-17 | 管線的 IFC 身分寫在哪 | A 不寫；B 寫物件名稱 `IfcType:GlobalId`；C 寫 UserText | **一般建議**：採 B。改成外參之後不會污染工作檔，原本「不要寫進 Rhino 物件」的顧慮已經消失。物件名稱在屬性面板直接看得到，發現衝突時可以直接把編號報給 BIM 端對；UserText 要多點兩下才看得到，而且外參物件的 UserText 存取比較彆扭。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-18 | 外參 `.3dm` 的建議位置 | A 不建議，完全自由；B 文件建議固定位置與固定檔名；C 指令強制指定路徑 | **一般建議**：採 B。**固定檔名是重點**：Worksession 的 Refresh 靠路徑不變，檔名每次帶日期就得重掛一次外參。建議 `_LoopFlow_Config/loopflow_R2M/inbound/`，每次覆寫同一個檔名。C 與 ED-28「第一版全手動」直接衝突。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-19 | 管線要不要自動上色 | A 不上色；B 依 IFC 類型上色；C 依 MEP 系統上色 | **輕鬆建議**：採 B。理由同 ED-16：系統資訊不保證拿得到，IFC 類型一定有。純粹為了肉眼分辨風管與水管，不影響任何資料正確性，所以強度低。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-20 | 件數過多時的行為 | A 不設限；B 超過門檻警告並可取消；C 硬上限拒絕讀取 | **一般建議**：採 B。真實 MEP IFC 可能上萬件，讀進來要等很久而且不知道是不是當掉了。C 會讓真的需要讀大檔的人完全沒有出路。門檻值等 B03 量到真實耗時再填，但警告機制要先寫進去。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-21 | IfcOpenShell 55 MB 怎麼隨套件發行 | A 全部打進 yak；B 首次執行時下載輪子；C 要求使用者自行安裝 | **強烈建議**：採 A，但**打包前必須先查 Rhino Package Manager 的體積限制**——這是 G02 的實質風險，不是形式問題。B 會讓沒有網路或在公司防火牆後面的電腦裝不起來，而且下載的二進位檔要驗簽章，否則是資安問題。C 直接違反 ED-09「不讓使用者 pip」。只有在 A 真的因體積被擋，才回頭評估 B。 | 尚未提供 | 尚未提供 | 待決定 |
| R2M-ED-29 | R2M 高程框的圖層名與欄位名 | A 沿用 LoopFlow 的圖層名與 UserText 欄位名；B 用 R2M 專屬名稱（例如 `R2M_Storey`）；C 可在 config 指定 | **強烈建議**：採 B。**擋 C01。** ED-26 已經決定兩邊資料獨立，如果連圖層名也一樣，同一個 `.3dm` 裡就會有兩組長得一模一樣的高程框，使用者分不出哪一組是 FFL、哪一組是 FL——這正是 ED-27 說會造成誤判的情況，而且誤差只有 3 到 5 公分，肉眼看不出來。名稱不同才看得出來是兩件事。C 增加設定面積卻沒解決混淆。 | 尚未提供 | 尚未提供 | 待決定 |

## 執行方式與已知問題

見 `開發任務與路徑.md`、`資料契約.md`「已知技術限制」。摘要：

1. A01 文件與 B01 Rhino 端針刺已做；本批補齊實作前規格。
2. 釘 IfcOpenShell 0.8.4.post1 py39 Windows 輪子；不可 pip 最新。
3. Revit／Archicad 連結測檔仍待家中實機。
4. **擋住 C01 開工的是 ED-07（圖層對 IFC 類型）、ED-13（物件掛哪一層）與 ED-29（高程框圖層與欄位名）**，三項不定就寫不出正確的 IFC。
5. Rhino 8 沒有掛載 Worksession 的程式介面（`Worksession.Attach` 要 Rhino 9），第一版改手動後不受影響。

## 下一步（使用者）

1. 讓 Claude 與 Codex 各讀一次 `wip/docs/` 六份文件與本表，填入自己的建議欄與強度。不要互看，分開填才有比對價值。
2. 三家填完後重跑 `wip/tools/fill_decision_table.py`，兩票以上同強度的列會自動填入決定並在 HTML 著色。
3. 用 `資料生態決策表.html` 覆核，特別看 ED-07、ED-13、ED-29 這三列——它們擋住 Models 開工。
4. 要改口就直接改決定欄後重跑工具；工具不會覆寫已寫上的決定。
