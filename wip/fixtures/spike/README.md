# R2M-B01 針刺產物

隔離 Rhino 8.35／CPython 3.9.10 寫出。輪子不在本目錄（見 repo `wip/.vendor/`，已 gitignore）。

| 檔 | 角色 |
|---|---|
| `R2M_spike_shell.ifc` | 建築殼：IfcWall + IfcCovering（天花） |
| `R2M_spike_pipe.ifc` | 管線參考：IfcPipeSegment |

公司這台沒有 Revit／Archicad。請之後用 BIM 軟體**連結** `R2M_spike_shell.ifc`（不要當原生檔轉來轉去），確認牆在下、天花在約 2.8 m。
