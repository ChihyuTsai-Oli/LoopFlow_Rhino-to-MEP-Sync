# LoopFlow R2M — 重構進度

- 建立日期：2026-09-21
- 目標版本：尚未發 tag
- 整合分支：`v1-development`（`d1a16c1`）
- 短期工作分支：`codex/v1-models-panel`
- 穩定回復點：`v1-development` `d1a16c1`
- 狀態：**面板依 3dm 檔名分開，有 Save／Load Config；下拉預設 Proxy。** Archicad：開啟當新檔或刪 Story 1／2 後 Update；不要 Merge 非整棟；單層缺件仍是 Proxy。未打 yak。真實 MEP IFC 尚未帶回。

## AI 接手入口

依序讀：`實作總覽.md` → `資料契約.md` → `工作流程.md` → `開發任務與路徑.md` → `系統設定.md` → 本文件。  
待決已清空，已決與比對見 `前期規劃/資料生態決策表.md`；指令候選見 `rhino指令.md`。  
開發節奏：**能做就做、需確認再停**。

## 驗證紀錄

| 日期 | 分支 / commit | 檢查 | 結果 | 限制 |
|---|---|---|---|---|
| 2026-09-21 | `main` | 公開 repo＋六份文件＋決策表 | 文件 | 無程式 |
| 2026-09-21 | `main` | MIT `LICENSE`；GitHub About 對齊系列 | 文件 | 尚未針刺 |
| 2026-09-21 | `codex/v1-ifcopenshell-spike` | B01：隔離 Rhino 8.35／py3.9.10；IfcOpenShell 0.8.4.post1；寫 shell／pipe IFC；鎖定網面；淨空 22.5 cm；RhinoCycles 仍可載入 | Rhino 端通過 | 公司電腦無 Revit／Archicad；未裝進 rhinocode site-packages；使用者正在編輯的 `.3dm` 未動 |
| 2026-09-21 | `codex/v1-spec-fill` | A02：單位／座標／幾何表示法／物件身分／樓層／Inbound 外參／config.json／r2m.log／validate 寫進契約；ED-13～ED-28 進決策表；補 BIM 端設定要點、測試策略、yak vendor 位置、B03 驗證步驟 | 文件 | 只有文件，無產品碼 |
| 2026-09-21 | `codex/v1-spec-fill` | 產三角網面版建築殼 `R2M_spike_shell_mesh.ifc`（`IfcTriangulatedFaceSet`），隔離 slot 讀回：牆 0–280 cm、天花 280–290 cm，各 8 頂點 12 面 | 通過 | 仍未經 BIM 端連結 |
| 2026-09-21 | `codex/v1-spec-fill` | 決策表改三家 AI 比對（Cursor／Claude／Codex），待決 16 項給 A／B／C 選項並填 Cursor 欄；新增 ED-29；移植 `fill_decision_table.py` 產生 HTML | 通過（工具跑出 3 表 29 列，filled=0 符合預期） | Claude／Codex 兩欄尚未提供，不足兩票故不自動裁決 |
| 2026-09-21 | `codex/v1-b03-archicad` | B03：家中把 `fixtures/spike` IFC 匯入 Archicad，沒有問題。測試 BIM 改暫以 Archicad 為準 | Archicad 匯入通過 | Revit 尚未測；未記錄 Archicad 版本與尺寸核對細節；真實 MEP IFC 尚未帶回 |
| 2026-09-21 | `codex/v1-b03-archicad` | 決策表填入 Claude 欄：待決 16 列全數給出強度與 A／B／C，工具 `--dry-run` 解析正確（16 列都抓到強度與選項）；只重產 HTML，未裁決 | 通過 | **Claude 欄非盲填**——Cursor 欄與它同檔，填寫時已看到，兩欄獨立性有限；Codex 欄仍為「尚未提供」，決定欄維持 16 列待決定 |
| 2026-09-21 | `codex/v1-b03-archicad` | 決策表填入 Codex 全部 16 列，補判斷依據與分歧條件；以 `--html-only` 同步閱讀版 | 文件檢查：16 列均有強度與選項，既有建議／決定保持不變 | 非盲填；未裁決、未執行 Rhino／BIM 或打包測試 |
| 2026-09-21 | `codex/v1-adopt-ed` | 使用者授權：兩家以上字母相同即採用。16 列三欄字母全數一致，寫入決定並回寫契約（含 ED-07／13／29） | 文件 | 非盲填；ED-15 數值、ED-20 門檻、ED-13 跨層測例、ED-21 打包體積仍待實測 |
| 2026-09-21 | `codex/v1-c01-core` | 開發順序改為 C01 先寫、Archicad 測試只排家中；新增不需 Rhino 的核心與單元測試 | `python -m unittest discover -s tests -t .`：17 passed | Archicad 真實 MEP 仍待家中；Rhino 指令尚未寫 |
| 2026-09-21 | `codex/v1-c01-models` | C01：`RMModels` 發布鏈（樓層／圖層類型／三角網面／pending→validate→atomic／來源還原）。最高層上界 `R2M_FL_Top`。系統 Python 單元測試 | `python -m unittest discover -s tests -t .`：30 ran、28 passed、2 skipped（IfcOpenShell 在系統 Python 缺 numpy，預期） | Rhino 實機未跑；Archicad 連結驗收待家中 |
| 2026-09-21 | `codex/v1-flatten-wip` | 把 `wip/` 下文件、原始碼、測試、指令、fixtures、tools 移到 repo 根目錄，刪除 `wip/`。已快進合入 `v1-development` | `python -m unittest discover -s tests -t .`：30 ran、28 passed、2 skipped | `.vendor/` 仍在本機、不進 Git |
| 2026-09-21 | `codex/v1-d01-inbound` | D01 `RMInbound`、E01 `RMOpen`；Models 發布前圖層→類型對照。件數門檻未填則不警告 | `python -m unittest discover -s tests -t .`：36 ran、32 passed、4 skipped | Rhino 實機未跑；真實 MEP fixture 未帶回；門檻數值等 B03 |
| 2026-09-22 | `codex/v1-storey-command` | C00 `RMStorey`：選全部框→點 1F→輸高程→點 RF，高程由框的 Z 推算、名稱自動編。圖層改 `R2M::Storey`，移除 `R2M_FL_Top`，最高層無上界。修 `RMModels` 跑完全選（`IsSelected` 是方法不是屬性，漏括號恆為真）。修訂 ED-13／ED-29、新增 ED-30 | `python -m unittest discover -s tests -t .`：48 ran、44 passed、4 skipped；`python -m compileall src commands` 無錯 | 見下一列實機結果 |
| 2026-09-22 | `codex/v1-storey-command` | 隔離 slot（Rhino 8，新 Rhino 實例，未開任何工作檔）以 stub 取代選取與彈窗，其餘走真實程式碼：① 證實 `obj.IsSelected` 未加括號回 `<bound method>`（恆為真），`IsSelected(False)` 未選取回 0、選取回 2；② `_Restore` 快照／還原——執行前選 1 個、過程中全選、還原後仍只剩那 1 個；執行前 0 個則還原後 0 個；③ `run_rmstorey` 對 5 個框（−300／0／300／600／900）編出 B1／1F／2F／RF／R2F，搬到 `R2M::Storey`，不相干的既有 UserText 保留；④ `read_storeys` 讀回一致；⑤ 手改 `R2M_FL=999` 被擋，訊息指出框實際在 300；⑥ 重跑覆寫正確；⑦ `GetObject` 屬性與 `ShowEditBox(title, message, defaultText, multiline, out text)->bool` 簽章與程式假設一致 | 全部通過 | **互動本身未驗**——`pick_curves` 與 `ShowEditBox` 是用 stub 取代的，實際點選與彈窗操作要你在 Rhino 裡跑一次。`RMModels` 完整發布鏈（需已存檔文件）未在本輪跑。容差（框的水平度 1e-6、高程驗算 1e-4）未經真實模型檢驗 |
| 2026-09-22 | `codex/v1-storey-command` | ED-31：高程框當匯出平面範圍。`classify_bbox_xy` 嚴格在內／完全在外／碰觸；`read_storeys` 帶回 XY 多邊形。RMStorey 選框前說明窗；RMModels 碰框擋住、框外同層跳過。單元測試含共邊、跨邊、包住框、凹形跨凹口 | `python -m unittest discover -s tests -t .`：54 ran、50 passed、4 skipped | 隔離 slot：矩形框 `(-50,-50)–(1050,850)` 抽出 4 頂點；內側盒 inside、貼邊盒 touch、x=2000 盒 outside。**未跑完整 RMModels 發布**（需已存檔）；說明窗與點選互動仍待人工跑 |
| 2026-09-22 | `codex/v1-storey-command` | ED-32：同一個 RMStorey 開頭選 WholeBuilding／PartialStoreys。非整棟指定一層名稱與高程，其餘依 Z 連續編號，不產生 RF、不補空樓層 | `python -m unittest discover -s tests -t .`：62 ran、58 passed、4 skipped | 隔離 slot stub：兩框 Z=1600／1920，基準 5F=1600 → `5F FL=1600`、`6F FL=1920`。模式選項與名稱彈窗互動仍待人工跑 |
| 2026-09-22 | `codex/v1-storey-command` | 實機 `Cancelled.` 不是框錯位：指令列 GetOption 在選完框後按 Enter 會被當成取消。改為先選框、再彈窗選 WholeBuilding／PartialStoreys；同高程的框改在選模式前擋住 | 62 ran、58 passed、4 skipped | 隔離 slot：三框 0／300／600 編成 1F／2F／RF；三框同 Z=0 在選模式前擋住。使用者開著的 `LoopFlow_R2M.3dm` 未連上 MCP，磁碟上的存檔 `//work` 沒有三條高程框 |
| 2026-09-22 | `codex/v1-storey-command` | 修 Rhino pythonnet 不接受 `Button(Text=...)`：改先建構再設 `.Text`。RMModels／RMStorey 模式／RMOpen 三處彈窗一併修 | 62 ran、58 passed、4 skipped | 隔離 slot：關鍵字建構仍 TypeError；幫手建出 `RMModels` 對話框，Publish／Cancel 文字正確。**未跑完整發布**，請再貼 `RMModels` 那行 |
| 2026-09-22 | `codex/v1-storey-command` | 修訂 ED-04／ED-07：只列最末端圖層；Select All／None；第一次全不勾；成功後還原面板；未選類型＝`IfcBuildingElementProxy` | 70 ran、66 passed、4 skipped | 隔離 slot：`M2D`／`M2D::Plan`／`Layout` 不列出；無紀錄全不勾且類型為 `(reference)`；還原 `WALL` 勾選＋`IfcWall`、`CEILING` 不勾。**請再跑 RMModels** |
| 2026-09-22 | `codex/v1-storey-command` | 第一次全不勾時按 Publish 會變成 `No layers selected.` 對話框改為沒勾圖層就不關、提示 Check at least one layer | 70 ran、66 passed、4 skipped | 隔離 slot：CheckBox.Checked 讀寫為一般 bool。請再跑 RMModels，先 Select All 或勾要匯出的層 |
| 2026-09-22 | `codex/v1-storey-command` | 低於最低層改跳過（非整棟 2F–4F 時 1F 物件不再擋住）。指令列列出 skipped below 件數。修訂 ED-13 | 70 ran、66 passed、4 skipped | 請再跑 RMModels；應能發布並看到 `Skipped below lowest storey` |
| 2026-09-22 | `codex/v1-storey-command` | 掛層改用框的幾何 Z，不再拿 `R2M_FL` 跟模型 Z 比。FL 仍寫進 IFC。空產品訊息帶 skip 件數。修訂 ED-13 | 71 ran、67 passed、4 skipped | 實機：FL=850／框 Z≈−53 時先前全部被當成低於 2F。框仍須比外牆大一圈且各層平面疊在一起；樓板底若低於框會被跳過或掛到下一層 |
| 2026-09-22 | `codex/v1-storey-command` | ScriptEditor 四個指令每次丟掉已載入的 `loopflow_r2m`。單層檔 11F FL=3520、框 Z=0 時，舊碼會把全部物件當低於 11F | 71 ran、67 passed、4 skipped | 隔離讀檔：新掛層約 831 件在框內、52 低於 11F、20 在框外。請再跑單層 `RMModels`，應看到 `frame Z=` |
| 2026-09-22 | `codex/v1-storey-command` | 使用者實機兩版都寫出 IFC。整棟：28 件、14 層（B2–R3F），標高 0–34.8 m。單層：792 件、11F／12F，標高 35.2／38.38 m。log：整棟跳過框外 2＋低於最低層 1；單層跳過低於最低層 33 | 讀 STEP：IFC4、三角網面件數與 log 一致 | 兩份改名放同一 `models/`（產品預設檔名仍是 `R2M.ifc`）。共用一份 `config.json`，單層 IFC 的專案名仍是「整棟」。未經 Archicad 連結 |
| 2026-09-22 | `codex/v1-storey-placement` | Archicad 合併後樓層高仍是預設 0／3／6 m：IFC 只寫了 `Elevation`，`ObjectPlacement` 全是 (0,0,0)。單層 792 件中 735 件是 Proxy。改寫樓層 placement Z＝FL、網面 Z 相對框高；Proxy 過半警告 | 單元測試；Archicad 重開尚未驗 | 須重跑 `RMModels` 才有新 IFC。**2026-09-22 換機：公司日不測**；回家再開針刺檔 |
| 2026-09-22 | `codex/v1-models-panel` | 家中 Archicad：整棟 Merge 樓層對但留下 Story 1／2；單層 Merge 會把 3F 對到 Ground Floor。刪 Story 1／2 後 Update 整棟／單層樓層高都對。單層缺件是 Proxy（例 52／75），不是 Z。RMModels：`panels` 依 3dm 檔名；Save／Load Config；Publish 前寫面板；下拉預設 Proxy；atomic 寫 config | `python -m unittest discover -s tests -t .`：82 ran、76 passed、6 skipped；`python -m compileall src commands` 無錯 | 須在家重跑 `RMModels` 才用到新面板。不補 0 m 空樓層。不合 `main`／`v1-development` |

## 下一步

1. **家中重跑 `RMModels`**（須卸載舊 `loopflow_r2m`）：整棟／單層各存各的面板；牆／板／天花改成對應類型後再出單層。
2. Archicad 收檔：檔案 → 開啟當新檔；或刪 Story 1／2 後 Update。不要 Merge 單層。
3. 公司日不測 Archicad、不合進 `main`／`v1-development`。不要裝 VisualARQ。

## 本輪未做

- `RMOpen`／`RMInbound` 實機（`RMStorey`／`RMModels` 兩版已出 IFC）
- yak
- Dropbox 測檔夾與環境變數
- 公開英文使用指南正文
- BIM 端：Archicad 開啟／Update 樓層高已對；單層缺件待改類型後重出。Revit 未測
- 真實 Archicad／Revit 匯出的 MEP IFC fixture（目前管線測檔是自己寫的）
